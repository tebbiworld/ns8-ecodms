#
# Restic include patterns for the ecoDMS module backup (paths relative to /srv).
# Without this file the NS8 backup would contain state/environment only.
# Syntax reference: https://pkg.go.dev/path/filepath#Glob
#
state/environment
volumes/ecodms-data
volumes/ecodms-backup
volumes/ecodms-restore
volumes/ecodms-scaninput
